name: ci-cd

on:
  workflow_dispatch:

jobs:
  code:
    runs-on: ubuntu-latest
    setps:
      - name: clone the code
        run: echo "clone the code"
  build:
    runs-on: ubuntu-latest
    setps:
      - name: build code
        run: echo "build code"
  test:
    runs-on: ubuntu-latest
    setps:
      - name: build test
        run: echo "build test"
  deploy:
    runs-on: ubuntu-latest
    setps:
      - name: deploy the code
        run: echo "deploy the code"
